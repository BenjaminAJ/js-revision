/*
1.
Create an array of your favorite colors and:

Print the array.
Access and print the second color in the array.
Change the last color in the array to "purple" and print the updated array.

2.
Create an array of numbers from 1 to 10. Use the length property to:

Print the total number of elements in the array.
Add a new number to the array and print the updated length.
Adding and Removing Elements

3.
Create an array of fruits: ['apple', 'banana', 'cherry']. Perform the following:
Add "orange" and "grape" to the end of the array using push.
Add "kiwi" to the beginning of the array using unshift.
Remove the last fruit using pop and print the updated array.
Remove the first fruit using shift and print the updated array.
Slicing and Splicing

4.
Create an array of numbers: [10, 20, 30, 40, 50, 60]. Perform the following:

Use slice to extract the numbers from index 2 to 4 and print the result.
Use splice to remove the number at index 3 and replace it with 35. Print the updated array.

5.
Create an array of animals: ['cat', 'dog', 'rabbit', 'elephant']. Perform the following:

Use splice to add "lion" and "tiger" at index 2.
Use splice to remove "rabbit" and replace it with "deer".
Using forEach

6.
Create an array of numbers: [1, 2, 3, 4, 5]. Use forEach to:

Print each number multiplied by 2.
Print the index and value of each element.

7.
Create an array of strings: ['hello', 'world', 'javascript']. Use forEach to:

Print each string in uppercase.
Print the length of each string.
Functions with forEach

8.
Write a declared function printElement that takes two arguments (element and index) and prints them. Use this function with forEach on an array of your choice.

9.
Write an arrow function doubleValue that doubles a number. Use this function with forEach on an array of numbers to print the doubled values.

10.
Create an array of student objects, where each object has a name and score. Example:
let students = [
    { name: 'Alice', score: 85 },
    { name: 'Bob', score: 92 },
    { name: 'Charlie', score: 78 }
];
Use forEach to print each student's name and score.
Use splice to add a new student at index 1.
Use splice to remove the student with the lowest score.

*/
